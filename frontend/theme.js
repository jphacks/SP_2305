// theme.js
import { extendTheme } from "@chakra-ui/react"

const theme = extendTheme({
  colors: {
    blue: {
      75: "#d4e8ff"
    },
    'regal-blue': '#4D8DA9',
  },
});

export default theme;
